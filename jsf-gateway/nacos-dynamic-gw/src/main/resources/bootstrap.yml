server:
  port: 9091
spring:
  application:
    name: nc-gateway
  cloud:
    nacos:
      config:
        server-addr: dev:8848
gw:
  data-id: nacos.gateway
  group: MYGROUP
management:
  endpoints:
    web:
      exposure:
        include: '*'